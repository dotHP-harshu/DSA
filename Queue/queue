Queue : 
        -- A queue is a linear data structure that follows the rule of FIFO (first in first out)
            FIFO:- The element added first will be removed first 
            --Enalogy : like a line of a ticket counter

Operations of queue :

1. Enqueue - Add an element to the rear
2. Dequeue - Remove an element from the front
3. Peek - Get the front element without removing it 
4. isFull - Check if the queue is full
5. isEmpty - Check if the queue is empty